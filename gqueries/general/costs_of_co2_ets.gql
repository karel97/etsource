# CO2 costs paid by power and hydrogen production

- query =
     SUM(V(FLATTEN(G(electricity_production), G(hydrogen_production)), "primary_co2_emission * (1-free_co2_factor) * takes_part_in_ets")) * AREA(co2_price)
- unit = euro
